extern fn printf(fmt: &u8, a: &u8, b: &u8) void;

struct Pair {
  first: []u8,
  second: []u8 = "seven",
}

fn main() i32 {
  let pair = Pair { first = "six" };
  printf("%s %s\n".ptr, pair.first.ptr, pair.second.ptr);
  return 0;
}

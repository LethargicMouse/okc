extern fn printf(fmt: &u8, a: &u8, b: &u8) void;

fn main() i32 {
  printf(
    "%s %s\n".ptr, 
    myPair.first.ptr, 
    myPair.second.ptr,
  );
  return 0;
}

struct Pair<a, b> {
  first: a,
  second: b,
}

let myPair = Pair {
  first = "six",
  second = "seven",
};

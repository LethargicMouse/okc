extern fn printf(fmt: &u8, a: &u8, b: &u8) void;

fn main() i32 {
  printf("%s %s\n", my_pair.first.ptr, my_pair.second.ptr);
  return 0;
}

struct pair<a, b> {
  first: a,
  second: b,
}

let my_pair = pair {
  first: "six",
  second: "seven",
};
